doctype html
html.h-100(lang="en")
  head
    title= Title
    // script(src=assetPath('main.css'))
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel="icon" href=src=assetPath('/icons/favicon.ico') type="image/x-icon")
    script.
      var gon = !{JSON.stringify(gon)};
  body.vh-100
    .d-flex.flex-column.h-100#chat
      // block content
    script(src=assetPath('main.js'))
